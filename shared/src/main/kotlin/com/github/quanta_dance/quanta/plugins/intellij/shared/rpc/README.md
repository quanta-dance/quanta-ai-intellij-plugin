# shared/rpc

Shared RPC API definitions and transport-neutral service contracts.

## Owns
- RPC interfaces shared by frontend and backend
- API shapes that define module boundaries

## Notes
This package should stay small and stable; document breaking changes before moving implementations.
