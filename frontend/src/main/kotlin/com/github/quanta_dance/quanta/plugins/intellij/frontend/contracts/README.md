# frontend/contracts

Frontend-side helpers for shared contracts and RPC use.

## Owns
- Frontend contract adapters
- UI-facing wrappers around shared request/response types

## Notes
Shared data shapes should stay in `shared`; this package only adapts them for UI usage.
